# lovable-eject configuration
# No environment variables are required to run the CLI tool.
# The variables below are only needed if running the web UI server.

# Web UI server port (default: 5174)
# WEB_SERVER_PORT=5174

# Web UI dev server port (default: 5175)
# WEB_DEV_PORT=5175
