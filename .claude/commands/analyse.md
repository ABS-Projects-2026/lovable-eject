---
description: Run the analyse command against a target Lovable project
---

Run the lovable-eject analyse command in dev mode:

1. Check that `npm install` has been run
2. Run `npm run dev -- analyse $ARGUMENTS` where $ARGUMENTS is the path provided
3. Review the output and summarise findings
4. If issues are found, suggest which to tackle first
